export const webpack = {
    devServer: {
        setupMiddlewares: (middlewares) => {
            // Add your custom middlewares here if needed
            return middlewares;
        },
    },
};
  