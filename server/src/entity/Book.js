class Book {
    constructor(id, title, writer, cover_image, point, tags) {
      this.id = id;
      this.title = title;
      this.author = writer;
      this.coverImage = cover_image;
      this.price = point;
      this.tags = tags;
    }
  }
  
  export default Book;
  